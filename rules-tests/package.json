{
  "name": "set-rules-tests",
  "private": true,
  "description": "firestore.rules ve storage.rules için deploy-oncesi emulator testleri (kalici degil, gerektiginde calistirilir).",
  "scripts": {
    "test": "node rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4"
  }
}
